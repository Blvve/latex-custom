\ProvidesPackage{santi}

%%%%% OPTIONS %%%%%
\DeclareOption{roman}{\renewcommand{\familydefault}{\rmdefault}}
\DeclareOption{sans-serif}{\renewcommand{\familydefault}{\sfdefault}}
\ExecuteOptions{sans-serif}

% the provide command thing is a shit solution to shit problem. If an option
% is provided, some of these commands are executed twice. The first time the
% command does not exist, the second one it does. So i need to provide it 
% at the beginning to not get any errors. There must be a better way though.
\providecommand{\pagesize}{} 
\DeclareOption{a4paper}{\renewcommand{\pagesize}{a4paper}}
 % Good size for tablets according to wikibooks.
\DeclareOption{tablet}{\renewcommand{\pagesize}{paperwidth=441pt, paperheight=666pt}}
\ExecuteOptions{a4paper}

\providecommand{\lang}{}
\DeclareOption{en}{\renewcommand{\lang}{UKenglish}}
\DeclareOption{es}{\renewcommand{\lang}{spanish}}
\ExecuteOptions{en}

\providecommand{\numthmwithin}{}
\DeclareOption{article}{\renewcommand{\numthmwithin}{section}}
\DeclareOption{report}{\renewcommand{\numthmwithin}{chapter}}
\ExecuteOptions{article}

\DeclareOption*{\PackageWarning{santi}{Unknown ‘\CurrentOption’}}
\ProcessOptions\relax


%%%%% SOME PACKAGES %%%%%
\usepackage{enumitem} % Useful options for itemize and customizing lists (e.g. 'noitemsep' or 'align=...')
\usepackage[hidelinks]{hyperref} % Include links. hidelinks to hide annoying red border around them.
\usepackage[dvipsnames]{xcolor} % Get some more color names.
\usepackage{mathtools}


%%%%% FONT %%%%%
\usepackage[T1]{fontenc} % Set font (output) encoding for tildes, etc.
% Font set at options!


%%%%% PAGE LAYOUT %%%%%
\usepackage[\pagesize]{geometry} % Set page size. May not work as expected without pdflatex.


%%%%% HEADERS/FOOTERS %%%%%
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{} % clear everything
% \renewcommand{\sectionmark}[1]{\markboth{#1}{}} % use these to change the text of the header, remove the number, etc ...
% \renewcommand{\subsectionmark}[1]{\markright{#1}}
\fancyhead[LE, RO]{\thepage}
\fancyhead[RE]{\nouppercase\leftmark}
\fancyhead[LO]{\nouppercase\rightmark}


%%%%% LANGUAGE %%%%%
\def\spanishoptions{spain}
\usepackage[\lang]{babel} % Use spanish or UKenglish as needed.


%%%%% MATH/THEOREMS %%%%%
\usepackage{amsmath, amssymb}
\usepackage[\numthmwithin]{cooltheorems} %My custom package for colored theorems.

\mathtoolsset{showonlyrefs=true} % Number equations iff they are referenced.



%%%%% COMMANDS %%%%%
\renewcommand{\mathbb}[1]{\mathbf{#1}} % use bold font instead of blackboard bold
\newcommand{\C}{\mathbb C} % complex numbers
\newcommand{\N}{\mathbb N} % natural numbers
\renewcommand{\P}{\mathbb P} % probability
\newcommand{\Q}{\mathbb Q} % rational numbers
\newcommand{\R}{\mathbb R} % real numbers
\newcommand{\Z}{\mathbb Z} % integers
\newcommand{\E}{\mathbb E} % expectation

\newcommand{\defeq}{\coloneqq}
\renewcommand{\d}{\mathrm d}
\newcommand{\norm}[1]{\left\lVert #1 \right\rVert}

\DeclareMathOperator{\ord}{ord}
\DeclareMathOperator{\res}{res}

\endinput
